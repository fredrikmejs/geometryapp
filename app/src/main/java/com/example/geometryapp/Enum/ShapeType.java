package com.example.geometryapp.Enum;

//Different kind of shape types. These are used if it's necessary to identify which type of shape is drawn.
//For example when drawing correct answer for kite (category 11. Complete figure from area) correct answer contains lines which indicates the height and width of the shape
public enum ShapeType {
    KITE,
    PARALLELOGRAM,
    TRAPEZOID
}
